

export const validateCart = (prevCart, item, cartTotal) => {
    const existingItem = prevCart.find(cartItem => cartItem.sku === item.sku);
    const type = ;
    console.log(prevCart);
    console.log(item);
}